#include "shell.h"

/**
 * _prompt - The prompt line in shell
 * @argv: first parameter for argument
 * @argc: second parameter for argument
 * Return: return NULL if not return 0
 */
int _prompt((argv,) *argc)
{
	return (0);
}

