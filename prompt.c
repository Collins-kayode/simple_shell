#include "shell.h"
/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	int i = 5;
	int j = 6;

	if (i == 0, i < j, i++)
		printf("my betty is good %d\n", i);
	else
	{
		printf("Am not good betty %d\n", i);
	}
	return (0);
}

